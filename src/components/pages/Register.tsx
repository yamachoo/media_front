import { RegisterForm } from '../uiParts/RegisterForm'

export const Register: React.FC = () => {
  return (
    <div className="h-screen flex items-center justify-center p-2">
      <RegisterForm />
    </div>
  )
}
